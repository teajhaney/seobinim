import { NavigationBar, Hero } from "@/component";
import React from "react";

const page = () => {
  return (
    <main className="relative">
		  <NavigationBar />
		  <Hero />
    </main>
  );
};

export default page;
