import { NavigationBar, Hero, Service } from "@/component";
import React from "react";

const page = () => {
  return (
    <main className="relative">
      <NavigationBar />
      <Hero />
      <Service />
    </main>
  );
};

export default page;
